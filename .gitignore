.claude/
.direnv/

# No Python remains in the tree, but a stray bytecode cache was tracked once
# by accident (bfb5d0c) and chezmoi then deployed the stale .pyc to every
# machine, so the patterns stay as a guard.
__pycache__/
*.pyc
