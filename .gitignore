.claude/
.direnv/

# uv runs executable_ai-commit-msg as a PEP 723 script, and CPython writes the
# bytecode cache beside the source. Tracked once by accident (bfb5d0c), which
# then deployed a stale .pyc to every machine through chezmoi.
__pycache__/
*.pyc
